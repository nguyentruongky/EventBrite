import React from 'react';
import {View, SafeAreaView, Text, TextInput} from 'react-native';
import {getFont, Weight} from '@fonts';

export default function SearchScreen() {
	return <View style={{ flex: 1, backgroundColor: 'white' }}>
	  
  </View>;
}
